---
// Astro Components
import { Image } from "astro:assets";

// Components
import Button from "@/components/Button.astro";

// Assets
import duetonePhoto from "@/assets/duetone_photo.jpg";
---

<script>
  import { addAnimationObserver } from "@/lib/observer.ts";

  addAnimationObserver("#volunteer .divider", "play", 0.2);
  addAnimationObserver(".volunteer__header", "play", 0.2);
</script>

<section id="volunteer">
  <div class="content-layout gap-24 xl:flex">
    <div class="mb-6">
      <div class="items-center justify-center md:flex">
        <h2 class="text-header volunteer__header slideRight">
          ¿Tienes una idea que<br />quieras compartir?
        </h2>
        <span class="divider growRight bg-secondary"></span>
      </div>
      <p class="my-6 w-full">
        ¡Todos tenemos algo que compartir! Te ofrecemos el espacio perfecto para
        transmitir tu conocimiento y experiencia en tecnología o crecimiento
        personal.
        <br />
        <br />
        Inscríbete y sé parte de nuestro calendario de charlas. Además, te apoyaremos
        a mejorar tu oratoria, elocuencia y habilidades para hablar en público, utilizando
        herramientas de storytelling que harán tu presentación inolvidable.
      </p>
      <Button
        type="secondary"
        text="Participar"
        href="https://forms.gle/jVPCujUznSvDL2Mz8"
        externalLink
      />
    </div>
    <Image
      class="max-h-fit"
      src={duetonePhoto}
      alt="Un grupo de tres personas, sentadas en una mesa mientras trabajan y comparten ideas."
    />
  </div>
</section>

<style>
  #volunteer {
    background-color: var(--primary-color);
    color: var(--secondary-color);
    padding: 160px 16px 100px;
  }

  .volunteer__header {
    color: var(--secondary-color);
    text-align: left;
    width: 100%;
    max-width: 400px;
  }

  @media (min-width: 1024px) {
    #volunteer {
      padding: 160px 64px 140px;
    }
  }
</style>
