<header class="header">
  <div class="header-layout">
    <h2 class="header_logo slideRightAnimation">ParchaTech</h2>
  </div>
</header>

<style>
  .header {
    background-color: var(--primary-color);
  }

  .header-layout {
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 16px;
  }

  .header_logo {
    padding: 42px 0px 48px;
    /* font-size: 32px; */
    font-size: 24px;
    font-weight: normal;
    font-family: var(--secondary-font);
    color: var(--secondary-color);
  }

  .slideRightAnimation {
    opacity: 0;
    transform: translateY(-15px);
    transition: transform, opacity;
    animation: slideRight 1s ease-in-out forwards;
  }

  @keyframes slideRight {
    0% {
      opacity: 0;
      transform: translateY(-15px);
    }

    100% {
      opacity: 1;
      transform: translateX(0%);
    }
  }

  @media (min-width: 1024px) {
    .header-layout {
      padding: 0 64px;
    }
  }

  @media (min-width: 1280px) {
    .header_logo {
      font-size: 32px;
    }
  }
</style>
